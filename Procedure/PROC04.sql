-- 사원번호 입력해 급여 2배로 변경하는 저장함수

CREATE OR REPLACE FUNCTION CAL_BONUS(
	VEMPNO IN EMP.EMPNO%TYPE)
RETURN NUMBER
IS
	VSAL NUMBER(7,2);
BEGIN
	SELECT SAL
	INTO VSAL
	FROM EMP01
	WHERE EMPNO=VEMPNO;

	RETURN (VSAL*2);
END;
/	